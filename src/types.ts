export enum InterestFrequency {
  MONTHLY = "MONTHLY",
  QUARTERLY = "QUARTERLY",
  ANNUALLY = "ANNUALLY",
  AT_MATURITY = "AT_MATURITY",
}

// declare global {
//   InterestFrequency = InterestFrequency
// }
